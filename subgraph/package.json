{
  "name": "truestamp-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ truestamp-subgraph",
    "create-local": "graph create --node http://localhost:8020/ truestamp-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ truestamp-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 truestamp-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.68.0",
    "@graphprotocol/graph-ts": "0.32.0"
  }
}
